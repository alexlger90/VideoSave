import Foundation

// AVURLAsset accepts this options dictionary key for HTTP request headers.
// Xcode 16.4 no longer exposes the old SDK symbol to Swift, so keep the
// string key locally instead of depending on that removed declaration.
let AVURLAssetHTTPHeaderFieldsKey = "AVURLAssetHTTPHeaderFieldsKey"
